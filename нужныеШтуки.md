String dataFilePath = "src/main/resources/data/";
Orchestrator orc = new Orchestrator();
JiraApi jira = new JiraApi("https://jira.cbr.ru");
String userName = orc.getJiraUser();
String password = orc.getJiraPassword();
//        String statusUrl = orc.getJiraApiTestCaseStatusUrl(); // Готово
//        String customFieldsUrl = orc.getJiraApiCustomFieldsUrl(); // Готово
//        String folderTreeUrl = orc.getJiraApiFolderTreeUrl(); // Готово
String testCasesUrl = orc.getJiraApiTestCasesUrl();
String maxWorkers = orc.getMaxWorkers();

//        try {
//            // TODO: Добавить подключение к удаленному серверу Оракл
//            // TODO: Во всех вызовах заменить SQLITE на Оракл
//
//            System.out.println("Получение данных о статусах ТК");
//            String statusesData = jira.getDataFromUrl(userName, password, statusUrl);
//            TestCaseStatusParser statusesParser = new TestCaseStatusParser();
//            List<TestCaseStatus> statusesFields = statusesParser.parse(statusesData);
//
//            System.out.println("Получение данных о каталогах ТК");
//            String foldersData = jira.getDataFromUrl(userName, password, folderTreeUrl);
//            FolderTreeParser folderParser = new FolderTreeParser();
//            List<FolderTreeExtractor.Result> foldersFields = folderParser.parse(foldersData);
//
//            System.out.println("Получение данных о компонентах ТК");
//            String componentsData = jira.getDataFromUrl(userName, password, customFieldsUrl);
//            CustomFieldParser componentsParser = new CustomFieldParser();
//            List<CustomField> componentsFields = componentsParser.parse(componentsData);
//
////            SqliteConnect connection = new SqliteConnect();
////            connection.connect("mydb.db");
////
////            SyncInfoRepository syncInfoRepository = new SyncInfoRepository(connection);
////            TestCaseStatusRepository statusesRepository = new TestCaseStatusRepository(connection);
////            statusesRepository.insertIntoTableStatuses(statusesFields);
////            syncInfoRepository.insertIntoTableSyncInfo("STATUSES");
////            System.out.println("Запись в SQLITE данных о каталогах");
////            FolderTreeRepository foldersRepository = new FolderTreeRepository(connection);
////            foldersRepository.insertIntoTableTkFolders(foldersFields);
////            syncInfoRepository.insertIntoTableSyncInfo("TKFOLDERS");
////            System.out.println("Запись в SQLITE данных о полях");
////            CustomFieldRepository customFieldsRepository = new CustomFieldRepository(connection);
////            customFieldsRepository.inserIntoTableComponents(componentsFields);
////            syncInfoRepository.insertIntoTableSyncInfo("CUSTOMFIELDS");
////
////            connection.disconnect();
//
//        } catch (Exception e) {
//            System.err.println("Произошла ошибка сетевого взаимодействия: " + e.getMessage());
//        }




После Select данных об ID, UPDATEDON нужно будет преобразовать TimeStamp даты с помощью (Instant instant = Instant.parse(rs.getString("updatedon")))